name: nginx-prometheus-shiny-exporter
version: 0
license: MIT

crystal: 0.27.0

dependencies:
  kemal:
    github: kemalcr/kemal
    branch: master

targets:
  nginx-prometheus-shiny-exporter:
    main: src/main.cr
